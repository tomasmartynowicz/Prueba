import pygame

class Personaje():                                             #El parametro object se pone para poder crear subclases apartir de esta clase.
    def __init__(self,imagen):                                          #La funcion __ini__ se ejecuta cuando se crea la clase.
        self.imagen = imagen           #El parametro self no se que significa pero tiene que ir.

